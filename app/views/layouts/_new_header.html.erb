<!-- Navigation -->
<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header page-scroll">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <%= link_to 'All A-Board Alliance', root_path, class: "navbar-brand page-scroll" %>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                <li class="hidden">
                  <%= link_to '', "#page-top"%>
                </li>
                <li>
                  <%= link_to yield(:nav_title1), yield(:nav_link1), class: "page-scroll" %>
                </li>
                <li>
                  <%= link_to yield(:nav_title2), yield(:nav_link2), class: "page-scroll" %>
                </li>
                <li>
                  <%= link_to yield(:nav_title3), yield(:nav_link3), class: "page-scroll" %>
                </li>
                <li>
                  <%= link_to yield(:nav_title4), yield(:nav_link4), class: "page-scroll" %>
                </li>
                <li>
                  <% if user_signed_in? %>
      							<li class="dropdown">
      								<%= link_to current_user.get_name.titleize, "#", "data-toggle" => "dropdown", :class=>"dropdown-toggle"%>
      								<ul class="dropdown-menu">
      									<li><%= link_to "My Account", user_path(current_user.id)%></li>
      									<% if site_admin_logged_in? %>
      										<li class="divider"></li>
      										<li><%= link_to "Categories", categories_path%></li>
      										<li><%= link_to "Users", users_path%></li>
      				                    <% end %>
      									<li class="divider"></li>
      									<li><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></li>
      								</ul>
      							</li>
      						<% else %>
      							<li><%= link_to "Sign in", new_user_session_path %></li>
      						<% end %>
                </li>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>
