<% if @parent_organization.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@parent_organization.errors.count, "error") %>.
    </div>
    <ul>
    <% @parent_organization.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>