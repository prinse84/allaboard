<% provide(:title, "Add a new event for " + @board.name + ": ") %>
<%= render 'shared/header' %>

<section>
	<div class="container">
    <div class="row">
      <div class="col-lg-12">
        <h3 class="section-heading">Add an event for <%= @board.name %></h3>
      </div>
    </div>

		<div class="row text-muted">
			<div class="col-lg-12">
			     <p>Use the form below to add a new event for this board.</p>
			</div>
		</div>

		<div class="row">
			<div class="col-md-8">
					<%= render 'form'%>
			</div>
		</div>
	</div>
</section>
